name: Deploy Static Site to EC2 for project

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Copy files to nginx directory
        run: |
          sudo cp -r * /var/www/html/
